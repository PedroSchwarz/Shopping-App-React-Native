class CartItem {
  constructor(quantity, itemPrice, itemTitle, total) {
    this.quantity = quantity;
    this.itemPrice = itemPrice;
    this.itemTitle = itemTitle;
    this.total = total;
  }
}

export default CartItem;
